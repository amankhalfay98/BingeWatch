const mongoCollections = require('../config/mongoCollections');
const movies = mongoCollections.movies;

let create = async () => {
    return 1; 
};

let update = async () => {
    return 1; 
};

let remove = async () => {
    return 1; 
};

module.exports = {
    create,
    update,
    remove
};