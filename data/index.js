const moviesData = require('./movies');
const usersData = require('./users');
const reviewsData = require('./reviews');

module.exports = {
	movies: moviesData,
	users: usersData,
	reviews: reviewsData,
};
